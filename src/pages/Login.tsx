import React from 'react';
import {Outlet} from "react-router-dom";

const Login = () => {
    return (
        <>
            <div>
                LOGIN
            </div>
            <Outlet />
        </>
    );
};

export default Login;
